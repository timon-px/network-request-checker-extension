import style from "./style.module.scss"

const Header = () => {
  return (
    <header className={style.header}>
      <h1 className={style.header_title}>External Network Requests</h1>
    </header>
  )
}

export default Header
